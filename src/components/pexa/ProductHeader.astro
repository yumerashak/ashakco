---
/* PexaHeader.astro */
const links = [
  { href: "/",          label: "Home" },     // Back to main site
  { href: "#features",  label: "Features" }, // Page anchor
  { href: "#gallery",   label: "Gallery" },  // Page anchor
  { href: "#stores",    label: "Download" }, // Page anchor
];
---
<style>
  :root {
    --header-bg: rgba(3, 3, 4, 0.85);
    --header-border: rgba(255, 255, 255, 0.08);
    --accent: #8D46E7;
    --text-main: #ffffff;
  }

  .topbar {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--header-bg);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--header-border);
    transition: all 0.3s ease;
  }

  .inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 12px 24px;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    column-gap: 12px;
  }

  /* --- BRAND LOGO --- */
  .brand {
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    transition: opacity 0.2s;
  }
  .brand:hover { opacity: 0.8; }
  
  /* Logo styling tailored for Pexa Icon + Wordmark if needed */
  .brand img {
    height: 38px;
    width: auto;
    border-radius: 8px; /* Optional: if using app icon */
    object-fit: contain;
    display: block;
  }
  .brand-text {
    font-family: 'Pixelify Sans', sans-serif;
    font-weight: 700;
    color: #fff;
    font-size: 20px;
    letter-spacing: 1px;
  }

  /* --- NAVIGATION (Center) --- */
  .nav {
    display: none;
    gap: 6px;
    align-items: center;
    justify-self: center;
    background: rgba(255,255,255,0.03);
    padding: 5px;
    border-radius: 100px;
    border: 1px solid rgba(255,255,255,0.05);
  }

  .nav-link {
    font-family: 'Pixelify Sans', sans-serif;
    font-size: 14px;
    font-weight: 500;
    color: #a1a1aa;
    text-decoration: none;
    padding: 8px 18px;
    border-radius: 100px;
    transition: all 0.3s ease;
  }

  .nav-link:hover {
    color: #fff;
    background: rgba(255,255,255,0.08);
    text-shadow: 0 0 10px rgba(255,255,255,0.3);
  }

  /* --- ACTION (Right) --- */
  .actions {
    display: none; 
    justify-self: end;
    align-items: center;
  }
  
  .btn-get {
    background: var(--accent);
    color: #fff;
    padding: 8px 20px;
    border-radius: 100px;
    text-decoration: none;
    font-weight: 600;
    font-size: 14px;
    transition: all 0.2s;
    box-shadow: 0 0 15px rgba(141, 70, 231, 0.4);
  }
  .btn-get:hover {
    transform: scale(1.05);
    background: #9d5cf5;
  }

  /* Responsive */
  @media (min-width: 768px) {
    .nav { display: flex; }
    .actions { display: flex; }
  }
</style>

<header class="topbar">
  <div class="inner">
    
    <!-- Left: Logo (Shows PEXA + Ashak Studios parent reference implies reliability) -->
    <a class="brand" href="/pexa" aria-label="Pexa Home">
      <!-- Kullanılacak logo. App icon veya wordmark olabilir -->
      <img src="/assets/pexa-icon.png" alt="Pexa" />
      <span class="brand-text">PEXA</span>
    </a>

    <!-- Center: Navigation -->
    <nav class="nav" aria-label="Pexa menu">
      {links.map(l => (
        <a href={l.href} class="nav-link">{l.label}</a>
      ))}
    </nav>

    <!-- Right: Get App Button -->
    <div class="actions">
      <a href="#stores" class="btn-get">Get App</a>
    </div>

  </div>
</header>